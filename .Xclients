#!/bin/bash

# 	$Id$	

# Save debugging output by display, its often useful.
exec > $HOME/.Xsessionlog/$DISPLAY 2>&1
set -x

# debugging info
date
env
xset q

# initial set-up

# disable the screensaver
xset s noblank

# slow the mouse down
xset m 3 8

if [ -f $HOME/.Xmodmap ]; then
    xmodmap .Xmodmap
fi

# additional fonts
#for i in /usr/openwin/lib/X11/fonts/*
#do    
#   xset fp+ $i >/dev/null 2>&1
#done

# system monitoring
#xload -geometry 160x80-240-0 -fg white -bg skyblue &

# various time pieces...
#moonclock -moonclock -geometry -510+5 &
#sunclock iconified geometry is 128xXX
#sunclock -iconic -icongeometry -310-245 &
#sunclock -iconic &
#xdaliclock -cycle -transparent -noseconds -geometry -400-0 &

# toys
#xsnow -nowind &
#xearth -ncolors 16 -label -labelpos +0+0 -bigstars 20 -night 15 -font lucidasans-bold-18 &
#nice -n 20 xscreensaver -install -cycle 2 -timeout 5 &
#xsetroot -cursor $HOME/.icons/hobbes.icon $HOME/.icons/hobbesmask.icon &

# notify me of new mail...
#xbiff -geometry 48x48-322-66 -volume 0 &
tkpostage -detailFont 8x16 -numberFont 10x20 &

# windows
nxterm -fn lucidasanstypewriter-14 -geometry 80x64+30+40 &
nxterm -fn lucidasanstypewriter-14 -geometry 80x64+810+40 &

nxterm -fn 6x10 -geometry 80x10 -e swatch -t /var/log/swatch &

enlightenment
#fvwm2
