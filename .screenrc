# Eric's iScreenRC
# Tue Jul 28 09:37:41 CDT 1992

# $Id$	

# start from home dir
chdir

# the invisible man
deflogin off

# change command char to ctrl-], I use ctrl-a in Emacs
#escape 
escape \

# NetHack mode
nethack on

# something is happening somewhere!
activity "window % summons you!~"

# Woof Woof
bell "Bark, Bark in window %"

# This is how one can set a reattach password:
# password ODSJQf.4IJN7E    # "1234"

# no annoying audible bell, please
vbell on

# detach on hangup
autodetach on

# don't display the copyright page
startup_message off

# emulate .logout message
pow_detach_msg "Screen session of \$LOGNAME \$:cr:\$:nl:ended."

# make the shell in every window a login shell
#shell -$SHELL

# autoaka testing
# shellaka '> |tcsh'
# shellaka '$ |sh'

# set every new windows hardstatus line to somenthing descriptive
# defhstatus "screen: ^En (^Et)"

# I have the biggest.. buffers of them all!
defscrollback 4096

# don't kill window after the process died
# zombie "^["

################
#
# default windows
#

# screen -t local 0
# screen -t mail 1 elm
# screen -t 40 2 rlogin faui40

# caption always "%3n %t%? @%u%?%? [%h]%?"
# hardstatus alwaysignore
# hardstatus alwayslastline "%w"

screen -t emacs 0 gnuclient -nw
screen -t neomort 1 tcsh
screen -t neomort 2 tcsh
screen -t neomort 3 tcsh
screen -t trantor 4 tcsh
screen -t trantor 5 tcsh
screen -t trantor 6 tcsh
screen -t trantor 7 tcsh
screen -t irc 8 dnalounge
screen -t irc 9 BitchX -c \#oit
screen -t "trantor logs" 10 swatch -t /var/log/syslog
screen -t "http logs" 11 tail -100f /var/log/httpd/access_log
screen -t "tantalus logs" 12 tcsh
#screen -t "tantalus logs" 12 ssh tantalus exec swatch -t /var/log/syslog
screen -t "incoming mail" 13 tcsh
#screen -t "incoming mail" 13 ssh tantalus exec tail -100f .procmail/log/current/current

# cron entry keeps these "current" symlinks up-to-date
#screen -t swatch 1 tail -f /var/log/swatch
#screen -t procmail 2 tail -f .procmail/log/current/current
#screen -t irc 7 undernet
