#	$Id$	

#Set on when debugging
VERBOSE=on
 
#Replace `mail' with your mail directory (Pine uses mail, Elm uses Mail)
MAILDIR=$HOME/Mail
     
#Directory for storing procmail log and rc files
PMDIR=$HOME/.procmail
     
MONTH=`date +%m`
YEAR=`date +%Y`
LOGFILE=$PMDIR/log.$MONTH

:0
* ^Subject: page me
| sendpage -p XXXXXXXXXX -s %s

:0 ic
* ?test ! -d $MAILDIR/lists/$YEAR/$MONTH
| mkdir $MAILDIR/lists/$YEAR/$MONTH

# handle mailing lists and etc
INCLUDERC=$PMDIR/rc.maillists

# try to catch spam
INCLUDERC=$PMDIR/rc.spam

# global copy of all mail that hasn't already been sent to the bitbucket
MONTHFOLDER=$MAILDIR/archive/mail-incoming/`date +%Y-%m`
:0 c
$MONTHFOLDER
